# Core ML / inference
torch>=2.2
# transformers 5.x for native ColPali processor registration + Gemma 3 support.
transformers>=5.0
accelerate
bitsandbytes

# ColPali via colpali-engine, INSTALLED FROM GITHUB SOURCE — PyPI 0.3.16 lags
# the latest LoRA-remapping fixes for transformers 5.x:
#   pip install git+https://github.com/illuin-tech/colpali
# (cannot pin a git ref here cleanly; the Colab notebook handles the install.)

# Baselines
sentence-transformers
open_clip_torch

# Vector index
faiss-cpu

# Demo UI
gradio>=4

# Metrics
evaluate
rouge-score
bert-score

# Data + utils
kagglehub
pandas
pillow
pyyaml
python-dotenv
tqdm
