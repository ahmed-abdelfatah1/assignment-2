# Core ML / inference
torch>=2.2
# transformers 4.49+ ships native ColPaliForRetrieval; we no longer need the
# colpali-engine library (which had the LoRA-key remapping issues).
transformers>=4.49
accelerate
bitsandbytes

# Baselines
sentence-transformers
open_clip_torch

# Vector index
faiss-cpu

# Demo UI
gradio>=4

# Metrics
evaluate
rouge-score
bert-score

# Data + utils
kagglehub
pandas
pillow
pyyaml
python-dotenv
tqdm
