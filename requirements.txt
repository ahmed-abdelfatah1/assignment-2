# Core ML / inference
torch>=2.2
transformers>=4.45
accelerate
bitsandbytes

# Vision-language retrieval (mandatory)
colpali-engine>=0.3
# colpali-engine 0.3+ uses torchao internally and rejects <0.16.0 at runtime.
torchao>=0.16.0

# Baselines
sentence-transformers
open_clip_torch

# Vector index
faiss-cpu

# Demo UI
gradio>=4

# Metrics
evaluate
rouge-score
bert-score

# Data + utils
kagglehub
pandas
pillow
pyyaml
python-dotenv
tqdm
